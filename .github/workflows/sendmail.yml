name: Email Notification

on:
  workflow_run:
    workflows:
      - CI Build
    types:
      - completed

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: vedhalakshmir@gmail.com
          password: Blossom@123
          subject: GitHub Action Failed
          body: Workflow run information
          to: vedhalakshmir@gmail.com
          from: GitHub Actions
